export class User {
    id: number;
    email: string;
    // tslint:disable-next-line:variable-name
    public_id: string;
    name: string;
    phone: string;
    password: string;
    // tslint:disable-next-line:variable-name
    dttm_add: Date;
    coupon: string;
}
